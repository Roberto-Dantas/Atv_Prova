const botaoClick = document.querySelector('.btn button').addEventListener('click', () => {
  const username = {['Roberto']}
  const password = document.getElementByID('password').value
  if(document.getElementById('username').value === 
})    
